<workflow>
1. Confirm that the slide is art-directed and not text/data/evidence dense.
2. Record all supplied assets. Mark fidelity-critical assets for preserved overlay, never model redrawing by default.
3. Select an original layout/style contract and calculate the final real-asset box from aspect ratio and detail level.
4. Generate a transparent corner-marker skeleton for the exact box, then generate only the visual background.
5. Overlay the original asset as an independent HTML/PPTX object and store prompt, references, geometry, provider metadata, and trace preview.
6. For editable reconstruction, retain visual master, clean plate, masks, scene manifest, A1/A2/B layer-route provenance, and zero-change-outside-mask evidence.
7. Keep status at `rendered_pending_manual_review` until a fresh rendered result is viewed.
</workflow>
