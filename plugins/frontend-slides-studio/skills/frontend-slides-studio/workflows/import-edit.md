<workflow>
1. Open the source through Studio. Inspect the reported normalization strategy and confidence.
2. High-confidence discrete pages may open as a deck. Confirm low-confidence sibling promotion; preserve continuous prose as document mode.
3. Use Browse to verify original scripts and animations before editing.
4. Use Edit for copy/properties and Move for geometry. Geometry must use individual CSS `translate`, `scale`, and `rotate` so existing animated `transform` remains intact.
5. Before detaching flow content, measure every box, insert placeholders, then detach relative to the final containing block.
6. Commit commands at gesture end. Keep exact content-addressed snapshots; explicit Save is the only source overwrite.
7. Reopen the saved artifact and verify history, IDs, media, navigation, and animation behavior.
</workflow>
